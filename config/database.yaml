db:
    drivers: { default: { driver: mysql, host: localhost, database: duxcms, username: root, password: root, port: '3306', prefix: app_ } }
redis:
    drivers: { default: { host: 127.0.0.1, port: 6379, timeout: 2.5, auth: null } }
